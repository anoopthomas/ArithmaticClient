<html>
  <head>
    <link rel="stylesheet" href="/css/style.css" type="text/css" media="screen" />
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script>
      //establish websocket connection with server
      var iosocket = io.connect('http://localhost:8080');
      $('#incomingChatMessages').append($('<li class="alert">Connected</li>'));
		
	  function calculate(operation)
	  {	
	      var operand1 = document.getElementById("operand1text");
	      var operand2 = document.getElementById("operand2text");
	      
	      // checking if operands are in numeric format	
	      if(isNaN(operand1.value+operand2.value)){
	      	alert("Please input operands in numeric format");
	      	return;
	      }
	      
	      //checking for division by zero error
	      if(operation==='d' && operand2.value==0){
	      	alert("Operand 2 cannot be 0 for division");
	      	return;
	      }	      
	  }
    </script>
  </head>
  <body>
  	<div align="center">
    <div id="oprns"><b>Arithmatic Operations</b></div><br>
    <label for="operand1">Operand 1 : </label><input type="text" id="operand1text">&nbsp;&nbsp;
    <label for="operand2">Operand 2 : </label><input type="text" id="operand2text"><br><br>
    <button type="button" onclick="calculate('a')">Add</button>&nbsp;
    <button type="button" onclick="calculate('s')">Subtract</button>&nbsp;
    <button type="button" onclick="calculate('m')">Multiply</button>&nbsp;
    <button type="button" onclick="calculate('d')">Divide</button>&nbsp;
    </div>
  </body>
</html>	